# General plugins (non-markdown)
{ ... }:
{
  # This file intentionally left minimal.
  # All general plugins are configured in default.nix extraPlugins.
  # This module exists for future expansion if plugins grow beyond
  # what fits comfortably in default.nix.
}
