monitor=,highrr,auto,auto

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = LIBVA_DRIVER_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia

general {
    gaps_in = 0
    gaps_out = 0

    border_size = 0

    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    resize_on_border = false
    allow_tearing = false

    layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more.
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    background_color = 0x1e1e2e # Catpucchin Mocha (Base)
    disable_hyprland_logo = true 
    enable_swallow = true
}

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
    kb_options = caps:swapescape
    kb_options = ctrl:nocaps

    repeat_delay = 300
    repeat_rate = 50

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

gestures {
    workspace_swipe = false
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


$mainMod = SUPER

# Core xmonad bindings
bind = $mainMod SHIFT, Return, exec, ghostty                    # MOD+Return = terminal
bind = $mainMod SHIFT, C, killactive,                  # MOD+Shift+C = kill window  
bind = $mainMod SHIFT, Q, exit,                        # MOD+Shift+Q = quit WM
bind = $mainMod, P, exec, rofi -show drun             # MOD+P = launcher
bind = $mainMod, Space, togglefloating,                # MOD+Space = toggle floating
bind = $mainMod SHIFT, Space, pseudo,                  # MOD+Shift+Space = pseudo (layout cycle)
bind = $mainMod, Tab, cyclenext,                       # MOD+Tab = cycle windows
bind = $mainMod SHIFT, Tab, cyclenext, prev            # MOD+Shift+Tab = cycle windows reverse

bind = $mainMod, H, movefocus, l                       # MOD+H = focus left
bind = $mainMod, J, movefocus, d                       # MOD+J = focus down
bind = $mainMod, K, movefocus, u                       # MOD+K = focus up  
bind = $mainMod, L, movefocus, r                       # MOD+L = focus right

bind = $mainMod SHIFT, H, movewindow, l                # MOD+Shift+H = move window left
bind = $mainMod SHIFT, J, movewindow, d                # MOD+Shift+J = move window down
bind = $mainMod SHIFT, K, movewindow, u                # MOD+Shift+K = move window up
bind = $mainMod SHIFT, L, movewindow, r                # MOD+Shift+L = move window right

# Master/stack management 
bind = $mainMod SHIFT, Return, layoutmsg, swapwithmaster  # MOD+Shift+Return = swap with master
bind = $mainMod, M, layoutmsg, focusmaster             # MOD+M = focus master window
bind = $mainMod SHIFT, M, layoutmsg, swapwithmaster    # MOD+Shift+M = alternative swap with master
bind = $mainMod, comma, splitratio, -0.1               # MOD+, = shrink master
bind = $mainMod, period, splitratio, +0.1              # MOD+. = expand master
bind = $mainMod SHIFT, J, layoutmsg, swapnext          # MOD+Shift+J = swap with next in stack
bind = $mainMod SHIFT, K, layoutmsg, swapprev          # MOD+Shift+K = swap with prev in stack

# Layout controls
bind = $mainMod, T, exec, hyprctl keyword general:layout "dwindle"     # MOD+T = tiled layout
bind = $mainMod, F, fullscreen, 1                      # MOD+F = fullscreen
bind = $mainMod SHIFT, F, fullscreen, 0                # MOD+Shift+F = real fullscreen

# Switch workspaces with mainMod + [1-9,0]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to workspace with mainMod + SHIFT + [1-9,0]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, Q, exec, hyprctl reload 
bind = $mainMod, W, killactive,
bind = $mainMod ALT, L, exec, waylock

bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
